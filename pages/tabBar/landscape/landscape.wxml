<view>
  <view wx:for="{{attractions}}" wx:for-index="idx" wx:key="item.name" class="attraction-des">
    <listItem distance="{{distanceData[idx]}}" coverImage="{{item.coverImage}}" dataSource="{{item}}" name="{{item.name}}" collectionData="{{collectionData}}" index="{{idx}}" bind:clickItem="goDetailPage">
    </listItem>
  </view> 
  
</view>
